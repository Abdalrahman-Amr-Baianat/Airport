<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{appName}} – Your verification code</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Invisible preheader text: shows in inbox preview -->
  <meta name="color-scheme" content="light dark">
  <meta name="supported-color-schemes" content="light dark">
  <style>
    /* Some clients respect <style> for media queries; all critical styles are inlined below */
    @media (max-width: 600px) {
      .container { width: 100% !important; }
      .card { padding: 24px !important; }
      .otp { letter-spacing: 6px !important; font-size: 28px !important; }
      .btn { display:block !important; width:100% !important; }
    }
    /* Dark mode helpers for clients that support it */
    @media (prefers-color-scheme: dark) {
      .bg { background-color: #0b0d12 !important; }
      .card { background-color: #151922 !important; color: #e6e9ef !important; }
      .muted { color: #9aa4b2 !important; }
      .otp { background:#0f1420 !important; border-color:#2a3242 !important; color:#e6e9ef !important; }
      .divider { border-color:#2a3242 !important; }
    }
  </style>
</head>
<body style="margin:0; padding:0; background:#f2f5f9;" class="bg">
  <!-- Preheader (hidden) -->
  <div style="display:none; overflow:hidden; line-height:1px; opacity:0; max-height:0; max-width:0;">
    Your {{appName}} verification code is {{otp}}. It expires in {{expiresInMinutes}} minutes.
  </div>

  <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="background:#f2f5f9;" class="bg">
    <tr>
      <td align="center" style="padding:32px 16px;">
        <table role="presentation" width="600" cellspacing="0" cellpadding="0" border="0" class="container" style="width:600px; max-width:600px;">
          <!-- Header -->
          <tr>
            <td align="left" style="padding: 8px 4px 16px 4px; font-family: -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica,Arial,sans-serif; color:#3b4354;">
              <span style="font-size:16px; font-weight:600; margin-left:8px; vertical-align:top; display:inline-block; padding-top:10px; color:#3b4354;">{{appName}}</span>
            </td>
          </tr>

          <!-- Card -->
          <tr>
            <td style="background:#ffffff; border-radius:16px; box-shadow:0 4px 20px rgba(16,24,40,0.06); padding:32px; font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica,Arial,sans-serif; color:#1f2937;" class="card">
              <h1 style="margin:0 0 8px 0; font-size:22px; line-height:1.3; font-weight:700;">Verify your email</h1>
              <p style="margin:0 0 16px 0; font-size:14px; line-height:1.6; color:#475569;" class="muted">
                Use the one-time code below to finish signing in to <strong>{{appName}}</strong>.
              </p>

              <!-- OTP block -->
              <div style="margin:20px 0 16px 0; padding:14px 18px; border:1px solid #e5e7eb; background:#f8fafc; border-radius:12px; display:inline-block;">
                <code style="font-size:30px; font-weight:800; letter-spacing:10px; color:#111827;" class="otp">{{otp}}</code>
              </div>

              <p style="margin:8px 0 0 0; font-size:13px; color:#64748b;" class="muted">
                This code expires in <strong>{{expiresInMinutes}} minutes</strong>. If you didn’t request this, you can safely ignore this email.
              </p>

              <!-- Divider -->
              <hr style="border:none; border-top:1px solid #e5e7eb; margin:24px 0;" class="divider">

              <!-- Button (optional deep link) -->
              <table role="presentation" cellspacing="0" cellpadding="0" border="0">
                <tr>
                  <td>
                    <a href="{{ctaUrl}}" class="btn"
                       style="background:#2563eb; color:#ffffff; text-decoration:none; padding:12px 18px; border-radius:10px; font-weight:600; font-size:14px; display:inline-block;">
                      Continue
                    </a>
                  </td>
                </tr>
              </table>

              <!-- Help -->
              <p style="margin:24px 0 0 0; font-size:12px; color:#94a3b8;" class="muted">
                Need help? Contact us at <a href="mailto:{{supportEmail}}" style="color:#2563eb; text-decoration:none;">{{supportEmail}}</a>.
              </p>
            </td>
          </tr>

          <!-- Footer -->
          <tr>
            <td align="center" style="padding:16px; font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica,Arial,sans-serif; color:#94a3b8; font-size:12px;">
              © {{year}} {{appName}}. All rights reserved.
            </td>
          </tr>

        </table>
      </td>
    </tr>
  </table>
</body>
</html>
